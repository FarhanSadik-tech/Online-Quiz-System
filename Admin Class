// Source code recreated from a .class file by IntelliJ IDEA
// (powered by FernFlower decompiler)
//

import java.io.PrintStream;
import java.util.List;
import java.util.UUID;

class Admin extends User {
    private String adminId;

    public Admin(String var1, String var2, String var3) {
        super(UUID.randomUUID().toString(), var2, var3);
        this.adminId = var1;
    }

    public String getAdminId() {
        return this.adminId;
    }

    public Quiz createQuiz(String var1) {
        return new Quiz(UUID.randomUUID().toString(), var1, "Description", 15);
    }

    public void addQuestion(Quiz var1, Question var2) {
        var1.questions.add(var2);
    }

    public void deleteQuiz(List<Quiz> var1, int var2) {
        ((Quiz)var1.get(var2)).isActive = false;
        var1.remove(var2);
        System.out.println("\ud83d\uddd1️ Quiz Deleted.");
    }

    public void viewAllResults(List<QuizTaker> var1) {
        for(QuizTaker var3 : var1) {
            for(Result var5 : var3.getResults()) {
                PrintStream var10000 = System.out;
                String var10001 = var3.getUsername();
                var10000.println("QuizTaker: " + var10001 + " | ID: " + var3.getUserId() + " | Quiz: " + var5.quiz.title + " | Score: " + var5.score);
            }
        }

    }
}
